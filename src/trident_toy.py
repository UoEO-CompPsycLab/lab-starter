"""Trident-G toy learner (change-point tracking).

This module is intentionally minimal. You can keep all code in notebooks at first,
then move stable functions here for reuse.

Planned:
- simulate_change_point_series(...)
- baseline_fixed_delta(...)
- baseline_adaptive_delta(...)
- trident_heavytail_learner(...)
"""

from __future__ import annotations
